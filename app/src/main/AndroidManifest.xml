<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gerenciarjogos">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GerenciarJogos">

        <activity android:name=".ui.CardJogo"></activity>
        <activity
            android:name=".ui.JogoDetalhe"
            android:theme="@style/Theme.GerenciarJogos.NoActionBar" />
        <activity
            android:name=".ui.CadastroJogos"
            android:label="@string/title_activity_cadastro_jogos"
            android:theme="@style/Theme.GerenciarJogos.NoActionBar" />
        <activity
            android:name=".ui.CadastroUsuariosActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".ui.login" />

        <activity android:name=".ui.MainActivity" android:theme="@style/Theme.GerenciarJogos.NoActionBar"/>

        <activity
            android:name=".ui.Splash"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>